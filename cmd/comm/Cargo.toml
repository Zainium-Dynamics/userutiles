[package]
name = "user_comm"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX comm — compare two sorted files line by line"
license = "GPL-3.0"
publish = false

[lib]
name = "user_comm"
path = "src/lib.rs"

[[bin]]
name = "comm"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
