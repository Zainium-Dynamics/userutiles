[package]
name = "user_fdisk"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX fdisk — list partition tables"
license = "GPL-3.0"
publish = false

[lib]
name = "user_fdisk"
path = "src/lib.rs"

[[bin]]
name = "fdisk"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }

[lints]
workspace = true
