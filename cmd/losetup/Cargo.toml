[package]
name = "user_losetup"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX losetup — set up and control loop devices"
license = "GPL-3.0"
publish = false

[lib]
name = "user_losetup"
path = "src/lib.rs"

[[bin]]
name = "losetup"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
