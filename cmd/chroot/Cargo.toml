[package]
name = "user_chroot"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX chroot — run command with special root directory"
license = "GPL-3.0"
publish = false

[lib]
name = "user_chroot"
path = "src/lib.rs"

[[bin]]
name = "chroot"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
