[package]
name = "user_login"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX login — begin a session on a terminal"
license = "GPL-3.0"
publish = false

[lib]
name = "user_login"
path = "src/lib.rs"

[[bin]]
name = "login"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
