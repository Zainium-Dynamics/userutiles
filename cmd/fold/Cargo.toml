[package]
name = "user_fold"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX fold — wrap each input line to fit specified width"
license = "GPL-3.0"
publish = false

[lib]
name = "user_fold"
path = "src/lib.rs"

[[bin]]
name = "fold"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
