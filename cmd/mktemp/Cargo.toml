[package]
name = "user_mktemp"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX mktemp — create a temporary file or directory"
license = "GPL-3.0"
publish = false

[lib]
name = "user_mktemp"
path = "src/lib.rs"

[[bin]]
name = "mktemp"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
