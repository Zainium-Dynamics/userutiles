[package]
name = "user_mount"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX mount — mount a filesystem"
license = "GPL-3.0"
publish = false

[lib]
name = "user_mount"
path = "src/lib.rs"

[[bin]]
name = "mount"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
