[package]
name = "user_umount"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX umount — unmount a filesystem"
license = "GPL-3.0"
publish = false

[lib]
name = "user_umount"
path = "src/lib.rs"

[[bin]]
name = "umount"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
