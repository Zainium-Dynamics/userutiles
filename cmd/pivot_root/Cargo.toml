[package]
name = "user_pivot_root"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX pivot_root — change the root filesystem"
license = "GPL-3.0"
publish = false

[lib]
name = "user_pivot_root"
path = "src/lib.rs"

[[bin]]
name = "pivot_root"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
