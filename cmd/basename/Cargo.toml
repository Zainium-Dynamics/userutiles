[package]
name = "user_basename"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX basename — strip directory and suffix from filenames"
license = "GPL-3.0"
publish = false

[lib]
name = "user_basename"
path = "src/lib.rs"

[[bin]]
name = "basename"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
