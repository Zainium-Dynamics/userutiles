[package]
name = "user_addpart"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX addpart — tell the kernel a partition was added"
license = "GPL-3.0"
publish = false

[lib]
name = "user_addpart"
path = "src/lib.rs"

[[bin]]
name = "addpart"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }

[lints]
workspace = true
