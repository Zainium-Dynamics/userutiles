[package]
name = "user_mkdir"
version = "0.1.0"
edition = "2021"
authors = ["Zainium Dynamics"]
description = "ZEX mkdir — redirects users to struct"
license = "GPL-3.0"
publish = false

[lib]
name = "user_mkdir"
path = "src/lib.rs"

[[bin]]
name = "mkdir"
path = "src/main.rs"

[lints]
workspace = true
