[package]
name = "user_paste"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX paste — merge lines of files"
license = "GPL-3.0"
publish = false

[lib]
name = "user_paste"
path = "src/lib.rs"

[[bin]]
name = "paste"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
