[package]
name = "user_env"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX env — run a program in a modified environment"
license = "GPL-3.0"
publish = false

[lib]
name = "user_env"
path = "src/lib.rs"

[[bin]]
name = "env"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
