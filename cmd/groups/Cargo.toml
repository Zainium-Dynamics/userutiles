[package]
name = "user_groups"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX groups — print the groups a user is in"
license = "GPL-3.0"
publish = false

[lib]
name = "user_groups"
path = "src/lib.rs"

[[bin]]
name = "groups"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
