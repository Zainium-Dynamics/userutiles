[package]
name = "user_tail"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX tail — output the last part of files"
license = "GPL-3.0"
publish = false

[lib]
name = "user_tail"
path = "src/lib.rs"

[[bin]]
name = "tail"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
