[package]
name = "user_sync"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX sync — synchronize cached writes to persistent storage"
license = "GPL-3.0"
publish = false

[lib]
name = "user_sync"
path = "src/lib.rs"

[[bin]]
name = "sync"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
