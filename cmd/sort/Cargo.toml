[package]
name = "user_sort"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX sort — sort lines of text files"
license = "GPL-3.0"
publish = false

[lib]
name = "user_sort"
path = "src/lib.rs"

[[bin]]
name = "sort"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
