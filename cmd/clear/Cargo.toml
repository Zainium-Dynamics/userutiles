[package]
name = "user_clear"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX clear — clear the terminal screen"
license = "GPL-3.0"
publish = false

[lib]
name = "user_clear"
path = "src/lib.rs"

[[bin]]
name = "clear"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
