[package]
name = "user_swapon"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX swapon — enable a swap area"
license = "GPL-3.0"
publish = false

[lib]
name = "user_swapon"
path = "src/lib.rs"

[[bin]]
name = "swapon"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
