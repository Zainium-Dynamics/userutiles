[package]
name = "user_factor"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX factor — factor numbers"
license = "GPL-3.0"
publish = false

[lib]
name = "user_factor"
path = "src/lib.rs"

[[bin]]
name = "factor"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
