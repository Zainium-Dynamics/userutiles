[package]
name = "user_lsmem"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX lsmem — list the ranges of available memory with their online status"
license = "GPL-3.0"
publish = false

[lib]
name = "user_lsmem"
path = "src/lib.rs"

[[bin]]
name = "lsmem"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }

[lints]
workspace = true
