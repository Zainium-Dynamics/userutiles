[package]
name = "user_mkfifo"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX mkfifo — make FIFOs (named pipes)"
license = "GPL-3.0"
publish = false

[lib]
name = "user_mkfifo"
path = "src/lib.rs"

[[bin]]
name = "mkfifo"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
