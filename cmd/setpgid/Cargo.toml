[package]
name = "user_setpgid"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX setpgid — run a program in a new process group"
license = "GPL-3.0"
publish = false

[lib]
name = "user_setpgid"
path = "src/lib.rs"

[[bin]]
name = "setpgid"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
