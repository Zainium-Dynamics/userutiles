[package]
name = "user_updatedb"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX updatedb — update a database for locate"
license = "GPL-3.0"
publish = false

[lib]
name = "user_updatedb"
path = "src/lib.rs"

[[bin]]
name = "updatedb"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
