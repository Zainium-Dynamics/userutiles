[package]
name = "user_partx"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX partx — tell the kernel about a device's partitions"
license = "GPL-3.0"
publish = false

[lib]
name = "user_partx"
path = "src/lib.rs"

[[bin]]
name = "partx"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
