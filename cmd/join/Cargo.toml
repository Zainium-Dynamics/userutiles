[package]
name = "user_join"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX join — join lines of two files on a common field"
license = "GPL-3.0"
publish = false

[lib]
name = "user_join"
path = "src/lib.rs"

[[bin]]
name = "join"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
