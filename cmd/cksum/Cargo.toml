[package]
name = "user_cksum"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX cksum — checksum and count the bytes in a file"
license = "GPL-3.0"
publish = false

[lib]
name = "user_cksum"
path = "src/lib.rs"

[[bin]]
name = "cksum"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
