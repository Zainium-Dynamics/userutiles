[package]
name = "user_mountpoint"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX mountpoint — see if a directory is a mountpoint"
license = "GPL-3.0"
publish = false

[lib]
name = "user_mountpoint"
path = "src/lib.rs"

[[bin]]
name = "mountpoint"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
