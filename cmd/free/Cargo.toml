[package]
name = "user_free"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX free — display amount of free and used memory"
license = "GPL-3.0"
publish = false

[lib]
name = "user_free"
path = "src/lib.rs"

[[bin]]
name = "free"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
