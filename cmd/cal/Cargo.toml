[package]
name = "user_cal"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX cal — display a calendar"
license = "GPL-3.0"
publish = false

[lib]
name = "user_cal"
path = "src/lib.rs"

[[bin]]
name = "cal"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
chrono = "0.4"

[lints]
workspace = true
