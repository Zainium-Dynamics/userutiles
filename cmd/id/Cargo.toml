[package]
name = "user_id"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX id — print real and effective user and group IDs"
license = "GPL-3.0"
publish = false

[lib]
name = "user_id"
path = "src/lib.rs"

[[bin]]
name = "id"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
