[package]
name = "user_uptime"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX uptime — tell how long the system has been running"
license = "GPL-3.0"
publish = false

[lib]
name = "user_uptime"
path = "src/lib.rs"

[[bin]]
name = "uptime"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
