//! user tree — guidance stub; Zainium uses `blueprint` instead of tree.
pub fn run() -> i32 {
    eprintln!("tree: on Zainium OS use `blueprint` instead of `tree`.");
    eprintln!();
    eprintln!(" blueprint is the Zainium project / directory structure tool.");
    eprintln!(" blueprint --help");
    eprintln!();
    eprintln!(" For simple mkdir-style paths, use `struct`:");
    eprintln!(" struct src/services/auth");
    2
}
