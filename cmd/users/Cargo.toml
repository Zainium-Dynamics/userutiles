[package]
name = "user_users"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX users — print login names of users currently logged in"
license = "GPL-3.0"
publish = false

[lib]
name = "user_users"
path = "src/lib.rs"

[[bin]]
name = "users"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"


[lints]
workspace = true
