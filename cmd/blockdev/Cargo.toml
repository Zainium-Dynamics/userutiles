[package]
name = "user_blockdev"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Zainium Dynamics"]
description = "ZEX blockdev — call block device ioctls from the command line"
license = "GPL-3.0"
publish = false

[lib]
name = "user_blockdev"
path = "src/lib.rs"

[[bin]]
name = "blockdev"
path = "src/main.rs"

[dependencies]
usercore = { path = "../../core" }
libc = "0.2"

[lints]
workspace = true
